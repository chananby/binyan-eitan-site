# עקרונות פיתוח - בניין איתן

> **🛑 הוראה לכל סוכן AI (קלוד גיט, סוכנים אחרים):**
> קרא את הקובץ הזה לפני כל משימה. אם משימה סותרת עקרון כאן - לדווח ולשאול את המשתמש לפני ביצוע.
>
> **קרא גם את `SYSTEM_MAP.md` בתחילת כל משימה** — מפת ניווט של המערכת (אזורים,
> זרימות, טבלאות, gotchas). חוסכת חקירה חוזרת ומצביעה על הקבצים הרלוונטיים.

---

## עקרונות מבנה

### 1. גודל קבצים - עד 400 שורות
קומפוננטה אחת = עד 400 שורות. מעל - להציע פיצול לפני שמוסיפים עוד.

- **דוגמה רעה:** AdminPortal.tsx הגיע ל-3,441 שורות. דרש refactoring של 3 שלבים.
- **דוגמה טובה:** WorkersTab.tsx (243 שורות), ProjectsTab.tsx (191 שורות).

### 2. קומפוננטה חדשה = קובץ חדש
פיצ'ר חדש = קובץ חדש בתיקייה המתאימה. לא להוסיף לקובץ קיים שמטרתו שונה.

- **דוגמה:** אם בעתיד יבוא "ניהול ספקים" - ייווצר `SuppliersTab.tsx` חדש, לא תוספת ל-AdminPortal.

### 3. fetch מחוץ ל-UI
קומפוננטות UI לא קוראות ל-API ישירות. ה-fetch קורה ב-parent דרך handler/callback שעובר כ-prop.

- **דוגמה טובה:** WorkersTab מקבל `onAddWorker` כ-prop. ה-fetch ב-AdminPortal.
- **דוגמה חריגה (לא לשכפל):** ProjectsTab עושה fetch inline לשיוך ממונה. נשאר כי עובד, אבל לא להוסיף עוד כאלה.

---

## עקרונות נתונים

### 4. שינויי DB דרך מיגרציה
כל שינוי schema (CREATE/ALTER/DROP) → קובץ ב-`supabase/migrations/` בפורמט `YYYYMMDD_<תיאור>.sql`.

**גמישות:** מותר תיקון זריז ב-Supabase Dashboard במקרי חירום, אבל **חובה לכתוב מיגרציה דיעבד ולדחוף ל-repo**. אסור שיהיו שינויי schema "רק בענן".

- **דוגמה למה זה חשוב:** טבלת tasks נוצרה ידנית ב-Dashboard עם schema שגוי. הבאג חי חודשיים בלי שמישהו ידע.

### 5. אחידות שמות בין קוד ל-DB
שם עמודה ב-DB = השם שהקוד שולח. בלי הפתעות.

- **דוגמה:** קוד שולח `task_name` → עמודה ב-DB נקראת בדיוק `task_name`. לא `name`, לא `title`.

---

## עקרונות תהליך

### 6. לפני commit
- `npm run build` חייב לעבור נקי, ללא warnings/errors.
- אם המשימה נגעה בפיצ'ר UI - להציע למשתמש בדיקת עשן ידנית (3-5 צעדים ספציפיים).

### 7. commit message מפורט
פורמט: `type(scope): short description`

- **דוגמה רעה:** `fix bug`
- **דוגמה טובה:** `fix(quotes): persist custom unit selection across reloads`

ב-body של ה-commit (כשנדרש): מה הייתה הבעיה, מה התיקון, למה התיקון הזה ולא אחר.

### 8. שמירה על מה שעובד
לפני שינוי בקוד שמשרת כמה מקומות - לבדוק את כל המקומות.

- **דוגמה:** `setTaskStatus` callback משותף בין Planning ל-Dashboard. שינוי בו דורש לבדוק את שניהם.

### 9. שינוי דגל/שדה משותף - חיפוש לפני סיום
כשמוסיפים או משנים שדה שמשפיע על כמה ממשקים (דגל סטטוס, הרשאה, סיווג, תעריף וכו'), להריץ grep על כל הצרכנים של אותו שדה ולוודא שכולם מטפלים בו באופן עקבי. לדווח אילו מקומות נמצאו ותוקנו - לא רק המקום שהוזכר במשימה.

- **דוגמה:** הוספת `attendance_exempt` ל-staff דרשה תיקון של ספירת "טרם החתימו" גם ב-AdminPortal וגם ב-ForemanPortal. רק grep גילה את שני המקומות.

הרציונל: מקור אמת אחד + חיפוש שיטתי במקום הסתמכות על זיכרון.

---

## עקרונות תקשורת

### 10. העדפה לרשימות על פני פסקאות וטבלאות
בעברית:

- אם יש לי 3+ פריטים להציג → רשימה ממוספרת או נקודות (לא טבלה)
- אם יש לי השוואה בין דברים → רשימה עם sub-bullets, לא טבלה
- טבלה רק אם יש 5+ פריטים והקשר ויזואלי חשוב
- פסקה לעולם לא תעלה על 3 משפטים
- בכל מקרה של ספק — לפרק לרשימה

### 11. שמות קבצים, נתיבים, ופונקציות באנגלית
תמיד בבלוק קוד מלא, לא בגרשיים בודדים. למשל:

- `src/lib/projects.ts:83`
- `getUserData()`
- `npm install`

---

## עקרונות טיפול בשגיאות

### 12. call-to-action מצביע על קוד חי
כשמוחקים פיצ'ר, `git grep` את שם הפיצ'ר ואת טקסט הכפתור בכל שפות ה-i18n — לא רק בקומפוננטה. מחיקת הכפתור לא מוחקת את המחרוזות שמפנות אליו.

- **דוגמה:** "דיווח חסר" נמחק, אבל הודעת `geoRequired` המשיכה להפנות אליו. גילינו רק כשעובד שלח צילום מסך מהשטח.

### 13. קוד אחד, הודעה אחת
כל כשל שהמשתמש יכול לגרום → קוד ברור והודעה ספציפית. כשל טכני פנימי → הודעה ידידותית + log מפורט בשרת. אל תקבץ הכל תחת `unknownError`, ואל תזלוג פרטים טכניים למשתמש.

- **דוגמה:** 8 מסלולי backend ב-`api/attendance` קיבצו למשתמש "שגיאה לא ידועה". עובד שנחסם על `location_required` לא ידע שהבעיה במיקום.

### הנחיה — שגיאה שמשביתה עובד ראויה לנראות
לא חוק גורף. שגיאה שמונעת מעובד פעולה מהותית (למשל חסימת החתמה) — שווה שתגיע לפאנל שהאדמין רואה, כדי שלא יהיה כשל שקט. שגיאה קטנה שהמשתמש יתגבר עליה בלחיצה חוזרת — לא.

---

## עקרונות UI

### 14. סולם טיפוגרפיה — 4 גדלים בלבד
כל טקסט במערכת שייך לאחד מארבעה טוקנים (`tailwind.config.js`). **אפס ערכים
שרירותיים (`text-[…]`) בקוד חדש**, ואין `text-xs` חדש — כל chrome חדש → `text-micro`.

| טוקן | גודל | שימוש |
|---|---|---|
| `text-body`    | 16px | הדגשה, מספרים ראשיים, prose |
| `text-content` | 15px | **רצפת התוכן** — כל מה שקוראים כדי לעבוד: שורות טבלה, ערכים, שמות, סכומים |
| `text-caption` | 14px | תוויות: כותרות עמודה, תוויות שדה, meta משני |
| `text-micro`   | 12px | **רצפת chrome** — badges, overline/uppercase, count pills. כלום מתחת ל-12px |

- **למה 12px רצפה:** עברית נקראת פחות טוב מלטינית בגדלים קטנים; מתחת ל-12px "שבור".
- **הרקע:** סריקה (יולי 2026) מצאה 18 גדלים שונים מתחת ל-15px ו-~40% מהקוד עוקף את
  הטוקנים. Tier 1 (טבלאות כסף יומיות) תוקן; Tier 2/3 בתור (ראה ROADMAP).
- `text-xs` (12px) של Tailwind נשאר רק כ-legacy — לא להוסיף חדש.

---

## הקשר טכני

- **Hosting:** Vercel (auto-deploy מ-main).
- **DB:** Supabase (Postgres + RLS + service_role API key).
- **Workflow:** GitHub Codespaces → git push to main → Vercel auto-deploys.
- **משתמש עובד רק ב-Codespaces** (cloud), לא במחשב מקומי.
- **תקשורת:** עברית, קצרה ומעשית.

---

**עדכון אחרון:** 6 ביולי 2026
