"use client";

import MathAppClient from "../MathAppClient";

// Grade 5–6 Senior — Percentages + Fractions
export default function SeniorPage() {
  return <MathAppClient topicIds={["percentages", "fractions"]} />;
}
